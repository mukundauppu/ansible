- name: To run and install nginx
  hosts: web
  become: yes
  tasks:
  - name: To install ngnix
      ansible.builtin.package:
      name: nginx
      state: present
  - name: To run ngnix
      name: nginx
      ansibe.builtin.service:
      state: started
      enabled: yes
    -
