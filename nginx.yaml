- name: To run and install nginx
  hosts: web
  become: yes
  tasks:
  - name: To install nginx
    ansible.build.in.package:
     name: nginx
     state: install
  - name: To start nginx
    ansible.build.in.service:
     name: nginx
     state: present
     enabled: yes
    
